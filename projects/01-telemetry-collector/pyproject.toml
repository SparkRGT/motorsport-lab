[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "f1-25-telemetry-collector"
version = "0.1.0"
description = "UDP telemetry collector for EA SPORTS F1 25"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]